//
//  SpliceCatastropheView.swift
//  CRM
//
//  Created by 吴海超 on 15/10/26.
//  Copyright © 2015年 吴海超. All rights reserved.
//

/*********************************************************
 *  gitHub:https://github.com/netyouli/WHC_AutoLayoutKit *
 *  本人其他优秀开源库：https://github.com/netyouli          *
 *********************************************************/


import UIKit

class SpliceCatastropheView : UIView , UIScrollViewDelegate , WHC_GestureImageViewDelegate{
 /// my attr start
private var breakfast_efficacious: [Any]!
private var injunction: Float = 0.0
private var coagulant_wholesale_wanton: Int = 0
var insentient_consume: UIImageView?
private var prime_circumvent_stinginess: [String: Any]?
var mistake_insentient: [String: Any]!
var satanic: [String: Any]!
var pernicious: NSString!
var regime_swedish: UIScrollView!
var fireplace_antitoxic_hypersensitive: UIView!
private var voucher_mexico_unsalable: [Any]?
var subsist_jerk: [Any]!
private var iron: UIImageView?
private var baggy: Double = 0.0
private var begrudge: Double = 0.0
private var slumber_restate: String?
var hinterland: String!
private var debut: String?
private var minor_unenlightened: CGFloat = 0.0
private var confide_withdrawn_maxim: UIScrollView!

/// my attr end


    fileprivate var images: [AnyObject]!;
    fileprivate var scrollView: UIScrollView!;
    fileprivate var index = 0;
    fileprivate var kZoomAniamtionTime = 0.3;
    fileprivate var currentIndex = 0;
    fileprivate var column = 0;
    fileprivate var currentImageView : ThoroughIdentificationView!;
    fileprivate let kImageViewTag = 10;
    fileprivate var backView: UIView!;
    fileprivate var rect: CGRect!;
    fileprivate var kShowLabelHeight:CGFloat = 20;
    fileprivate var showLabel: UILabel!;
 /// my attr start

static func incomplete_hoax_atrophy(cotton: [String: Any]!, huckster_derangement: Int) -> Float {
     var career: Float = 5663.0
     var plastic: String! = "absolute"
     let snatch_duel: Float = 9680.0
     var lawyer: String! = "deletion"
     let devious: CGFloat = 6667.0
    var gourmand:Float = 0
    career -= snatch_duel
    gourmand -= career
    gourmand *= snatch_duel
    gourmand += Float(devious)

    return gourmand

}



static func bugaboo_dissimilar_astound(acquittal: Bool, locate_cataract: [String: Any]!) -> [String: Any]! {
     let civility: Float = 5877.0
     let averagely_paralyse: Double = 6480.0
     var translator_girder: Float = 3536.0
     let nomad_inadvertently: Double = 5192.0
     let yielding_highbrow: Double = 2369.0
     let impossible_thursday: Double = 6030.0
     let suggestible_earthquake: String! = "define"
    var spackle_allegory :[String: Any]! = [String: Any]()
    spackle_allegory.updateValue(averagely_paralyse, forKey:"beloved")
    spackle_allegory.updateValue(nomad_inadvertently, forKey:"hieroglyph")
    spackle_allegory.updateValue(yielding_highbrow, forKey:"reprehend")

    return spackle_allegory

}



/// my attr end

    class func show(_ images: [AnyObject] ,
                    index: Int ,
                    item: UIView ,
                    column: Int) -> SpliceCatastropheView{
 /// my attr start

         self.incomplete_hoax_atrophy(cotton:["glutinous":[907, 646, 477]], huckster_derangement:7554)
         self.bugaboo_dissimilar_astound(acquittal:true, locate_cataract:["parliament":149, "viscid":265])
/// my attr end


        return SpliceCatastropheView(frame: UIScreen.main.bounds,
            images: images ,
            index: index,
            rect: item.convert(item.bounds, to: (UIApplication.shared.delegate?.window)!),
            column: column);
    }
    
    init(frame: CGRect ,
        images: [AnyObject] ,
         index: Int ,
         rect: CGRect ,
        column: Int) {
        super.init(frame: frame);
        self.images = images;
        self.index = index;
        self.currentIndex = index;
        self.column = column;
        self.rect = rect;
        self.oatmeal_unless();
    }
    
    required init?(coder aDecoder: NSCoder) {
        super.init(coder: aDecoder);
    }
 /// my attr start

 func jug_oclock_menace(integration: Float, pit: String!, division_reverent: CGFloat) -> UIImageView! {
     var fold: UIButton! = UIButton(frame:CGRect(x: 20, y: 416, width: 0, height: 0))
     let back: [Any]! = [670, 481]
     var gaiety: Int = 1880
     let pretension: UILabel! = UILabel(frame:CGRect.zero)
     var satisfactory_aria: Float = 8297.0
     let cane_banker: UILabel! = UILabel()
     let impermanent_donate: String! = "specification"
     let defalcate: UIImageView! = UIImageView(image:UIImage(named:"persuade"), highlightedImage:UIImage(named:"redemptive"))
     let powder: NSString! = "footstep"
     let elevation_thankful: UILabel! = UILabel(frame:CGRect(x: 96, y: 79, width: 0, height: 0))
    var pillow_bauxite_verdant = UIImageView(frame:CGRect.zero)
    fold.alpha = 0.3;
    fold.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    fold.frame = CGRect(x: 83, y: 122, width: 0, height: 0)
    fold.setTitle("", for: .normal)
    fold.setBackgroundImage(UIImage(named:"packed_fireman_temperately"), for: .normal)
    fold.titleLabel?.font = UIFont.systemFont(ofSize:18)
    fold.setImage(UIImage(named:"modified"), for: .normal)
    pretension.frame = CGRect(x: 141, y: 309, width: 0, height: 0)
    pretension.alpha = 1.0;
    pretension.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    pretension.text = ""
    pretension.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    pretension.textAlignment = .right
    pretension.font = UIFont.systemFont(ofSize:16)
    cane_banker.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    cane_banker.alpha = 0.4
    cane_banker.frame = CGRect(x: 145, y: 109, width: 0, height: 0)
    cane_banker.textAlignment = .right
    cane_banker.font = UIFont.systemFont(ofSize:11)
    cane_banker.text = ""
    cane_banker.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    defalcate.alpha = 0.9;
    defalcate.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    defalcate.frame = CGRect(x: 182, y: 25, width: 0, height: 0)
    defalcate.image = UIImage(named:"reputation_foray_service")
    defalcate.contentMode = .scaleAspectFit
    defalcate.animationRepeatCount = 3
    elevation_thankful.frame = CGRect(x: 113, y: 235, width: 0, height: 0)
    elevation_thankful.alpha = 0.9;
    elevation_thankful.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    elevation_thankful.textAlignment = .right
    elevation_thankful.font = UIFont.systemFont(ofSize:15)
    elevation_thankful.text = ""
    elevation_thankful.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    pillow_bauxite_verdant.alpha = 0.0;
    pillow_bauxite_verdant.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    pillow_bauxite_verdant.frame = CGRect(x: 132, y: 195, width: 0, height: 0)
    pillow_bauxite_verdant.contentMode = .scaleAspectFit
    pillow_bauxite_verdant.animationRepeatCount = 0
    pillow_bauxite_verdant.image = UIImage(named:"ballast_counsel")

    return pillow_bauxite_verdant

}



 func maneuver_shorten_unsettling(hospitality: [Any]!, continuously_peaky: Bool) -> Int {
     let dune: Double = 7735.0
     let spade: Float = 1299.0
     var blowhard_univocal: String! = "ratio"
     var voltage_enactment: CGFloat = 6548.0
     let santaclaus_for: Bool = false
     var daughter_in_law_pioneer: Float = 3982.0
    var cheap_native_halfhearted:Int = 0
    cheap_native_halfhearted *= Int(dune)
    cheap_native_halfhearted /= max(Int(spade), 1)

    cheap_native_halfhearted += Int(voltage_enactment)
    cheap_native_halfhearted *= Int(santaclaus_for ? 60 : 79)
    daughter_in_law_pioneer += spade
    cheap_native_halfhearted += Int(daughter_in_law_pioneer)

    return cheap_native_halfhearted

}



 func bewitch_post_office_regardless(widespread: NSString!, serene: NSString!) -> NSString! {
     let definitely: Bool = true
     let harrow: Float = 2546.0
     let modification: CGFloat = 8033.0
     let fig: Bool = false
     let highly_reproof: Bool = false
    var ingratiating_paris: NSMutableString! = NSMutableString()

    return ingratiating_paris

}



/// my attr end

    
    
    fileprivate func oatmeal_unless() {
 /// my attr start

         self.jug_oclock_menace(integration:4015.0, pit:"severity", division_reverent:2210.0)
         self.maneuver_shorten_unsettling(hospitality:[895, 760], continuously_peaky:true)
         self.bewitch_post_office_regardless(widespread:"authorization", serene:"disruptive")
/// my attr end

        self.backView = UIView(frame: UIScreen.main.bounds);
        self.backView.backgroundColor = UIColor.black;
        self.backView.alpha = 0;
        UIApplication.shared.delegate?.window??.addSubview(self.backView);
        
        self.backgroundColor = UIColor.clear;
        self.scrollView = UIScrollView(frame: self.bounds);
        self.scrollView.delegate = self;
        self.scrollView.showsHorizontalScrollIndicator = false;
        self.scrollView.showsVerticalScrollIndicator = false;
        self.addSubview(self.scrollView);
        if self.images.count > 0 {
            for (i , image) in self.images.enumerated() {
                let imageScrollView = UIScrollView(frame: CGRect(x: CGFloat(i) * self.width(), y: 0, width: self.width(), height: self.height()));
                let imageView = ThoroughIdentificationView(frame: imageScrollView.bounds);
                imageView.delegate = self;
                if self.currentIndex == i {
                    self.currentImageView = imageView;
                }
                imageView.tag = kImageViewTag;
                imageScrollView.addSubview(imageView);
                imageScrollView.delegate = self;
                imageScrollView.isMultipleTouchEnabled = true;
                imageScrollView.minimumZoomScale = 1.0;
                imageScrollView.maximumZoomScale = 2.5;
                imageScrollView.backgroundColor = UIColor.clear;
                imageScrollView.tag = i;
                self.scrollView.addSubview(imageScrollView);
                var imageObject: UIImage!;
                if image is UIImage {
                    imageObject = image as! UIImage;
                }else if image is String {
                    let imageName: String = image  as! String;
                    imageObject = UIImage(named: imageName);
                }
                if imageObject.size.width < imageScrollView.width() {
                    imageView.setWidth(imageObject.size.width);
                }
                if imageObject.size.height < imageScrollView.height() {
                    imageView.setHeight(imageObject.size.height);
                }
                imageView.setXy(CGPoint(x: (imageScrollView.width() - imageView.width()) / 2.0, y: (imageScrollView.height() - imageView.height()) / 2.0));
                imageView.image = imageObject;
            }
            self.scrollView.isPagingEnabled = true;
            self.scrollView.contentSize = CGSize(width: CGFloat(self.images.count) * self.scrollView.width(), height: self.scrollView.height());
            self.scrollView.setContentOffset(CGPoint(x: CGFloat(self.index) * self.width(), y: 0), animated: false);
            
            self.showLabel = UILabel(frame: CGRect(x: 0, y: self.height() - kShowLabelHeight, width: self.width(), height: kShowLabelHeight));
            self.showLabel.text = "\(self.images.count) / \(self.index + 1)";
            self.showLabel.textColor = UIColor.white;
            self.showLabel.textAlignment = .center;
            self.addSubview(self.showLabel);
            UIApplication.shared.delegate?.window??.addSubview(self);
            
            var rc = CGRect(x: 0, y: 0, width: self.currentImageView.width(), height: self.currentImageView.height());
            rc.origin = CGPoint(x: (self.width() - currentImageView.width()) / 2.0, y: (self.height() - currentImageView.height()) / 2.0);
            self.currentImageView.frame = self.rect;
            UIView.animate(withDuration: kZoomAniamtionTime, delay: 0, options: .curveEaseIn, animations: { () -> Void in
                self.currentImageView.frame = rc;
                self.backView.alpha = 1.0;
                }, completion: nil);
        }
    }
 /// my attr start

 func pretence_name_lover(ventilation: String!, gutter_sweet: CGFloat) -> UILabel! {
     var banal: UILabel! = UILabel()
     var response: UILabel! = UILabel(frame:CGRect.zero)
     let remembrance: Int = 1745
     var moon_harry: CGFloat = 3524.0
     let bend: String! = "charlatan"
     let palace_curt: [Any]! = [667, 865, 204]
     let farce: String! = "instantly"
     var favourite: UIView! = UIView()
     let mania_sinecure: String! = "nebula"
     var thanks: [Any]! = [650, 976]
     let skin_stark: [Any]! = [163, 567]
     let union: [Any]! = ["offer"]
     var trammel: Int = 6543
     let pertinence: [String: Any]! = ["savory":"punch"]
     let modesty: CGFloat = 133.0
     var sponsorship: Bool = false
     var homogeneous_crust: UIImageView! = UIImageView(image:UIImage(named:"circulate"))
     var incarcerate: Int = 4307
    var vow_script_exhume:UILabel! = UILabel(frame:CGRect.zero)
    vow_script_exhume.font = UIFont.systemFont(ofSize:10)
    vow_script_exhume.text = ""
    vow_script_exhume.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    vow_script_exhume.textAlignment = .right
    vow_script_exhume.alpha = 0.1;
    vow_script_exhume.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    vow_script_exhume.frame = CGRect(x: 54, y: 40, width: 0, height: 0)
    banal.alpha = 0.2;
    banal.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    banal.frame = CGRect(x: 57, y: 15, width: 0, height: 0)
    banal.font = UIFont.systemFont(ofSize:12)
    banal.text = ""
    banal.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    banal.textAlignment = .left
    response.alpha = 0.9;
    response.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    response.frame = CGRect(x: 318, y: 252, width: 0, height: 0)
    response.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    response.textAlignment = .right
    response.font = UIFont.systemFont(ofSize:17)
    response.text = ""
    favourite.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    favourite.alpha = 0.9
    favourite.frame = CGRect(x: 172, y: 231, width: 0, height: 0)
    homogeneous_crust.alpha = 0.3;
    homogeneous_crust.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    homogeneous_crust.frame = CGRect(x: 50, y: 125, width: 0, height: 0)
    homogeneous_crust.contentMode = .scaleAspectFit
    homogeneous_crust.animationRepeatCount = 0
    homogeneous_crust.image = UIImage(named:"luck_program")

    return vow_script_exhume

}



 func measurement_prefigure_badminton(striate_fleece: [Any]!, indication_fledge: Int, successive_apprise: NSString!) -> Double {
     var caustic: String! = "under"
     let inexplicit: Float = 6933.0
     let hereof_hue: String! = "weasel"
     var abominable_six: Bool = true
     var furor_acorn: Double = 8634.0
     var suite: String! = "wagon"
     var search_confederacy: Bool = true
     var luxurious: Bool = false
     let choir: Bool = true
     let shore: Bool = true
     let phase: Int = 1501
     var patriotism: Double = 7703.0
    var salubrious_lapidary_affection:Double = 0
    salubrious_lapidary_affection /= max(Double(inexplicit), 1)
    abominable_six = true
    salubrious_lapidary_affection += Double(abominable_six ? 5 : 4)
    furor_acorn /= 61
    salubrious_lapidary_affection += furor_acorn
    search_confederacy = true
    salubrious_lapidary_affection *= Double(search_confederacy ? 52 : 15)
    luxurious = false
    salubrious_lapidary_affection += Double(luxurious ? 33 : 82)
    salubrious_lapidary_affection /= Double(choir ? 82 : 88)
    salubrious_lapidary_affection += Double(shore ? 61 : 39)
    salubrious_lapidary_affection /= max(Double(phase), 1)
    patriotism += furor_acorn
    salubrious_lapidary_affection *= patriotism

    return salubrious_lapidary_affection

}



 func subdue_unimpressive_revelation(profound: String!, watchful: String!, engineer: Double) -> UIView! {
     var admire_deposition: UILabel! = UILabel()
     let glowing: UIImageView! = UIImageView(frame:CGRect.zero)
     var callous: UIView! = UIView()
     var thrash: UIButton! = UIButton()
     let perceive_fulcrum: String! = "authority"
     var jocund_respectfully: Bool = true
    var web_reprieve_selfish = UIView(frame:CGRect.zero)
    web_reprieve_selfish.alpha = 0.7;
    web_reprieve_selfish.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    web_reprieve_selfish.frame = CGRect(x: 133, y: 311, width: 0, height: 0)
    admire_deposition.frame = CGRect(x: 105, y: 122, width: 0, height: 0)
    admire_deposition.alpha = 0.7;
    admire_deposition.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    admire_deposition.font = UIFont.systemFont(ofSize:19)
    admire_deposition.text = ""
    admire_deposition.textColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    admire_deposition.textAlignment = .left
    web_reprieve_selfish.addSubview(admire_deposition)
    glowing.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    glowing.alpha = 0.8
    glowing.frame = CGRect(x: 320, y: 220, width: 0, height: 0)
    glowing.animationRepeatCount = 10
    glowing.image = UIImage(named:"sympathize_inkling_exhaust")
    glowing.contentMode = .scaleAspectFit
    web_reprieve_selfish.addSubview(glowing)
    callous.frame = CGRect(x: 283, y: 146, width: 0, height: 0)
    callous.alpha = 0.1;
    callous.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    web_reprieve_selfish.addSubview(callous)
    thrash.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    thrash.alpha = 0.6
    thrash.frame = CGRect(x: 145, y: 304, width: 0, height: 0)
    thrash.setTitle("", for: .normal)
    thrash.setBackgroundImage(UIImage(named:"abominable_thoughtful"), for: .normal)
    thrash.titleLabel?.font = UIFont.systemFont(ofSize:17)
    thrash.setImage(UIImage(named:"expectant_artery_estimable"), for: .normal)
    web_reprieve_selfish.addSubview(thrash)

    return web_reprieve_selfish

}



/// my attr end

    
    fileprivate func flannel_syllogism_wither() -> CGRect {
 /// my attr start

         self.bewitch_post_office_regardless(widespread:"label", serene:"procrastinate")
         self.pretence_name_lover(ventilation:"precipitant", gutter_sweet:1394.0)
         self.measurement_prefigure_badminton(striate_fleece:[968, 320, 838], indication_fledge:3014, successive_apprise:"monstrous")
         self.subdue_unimpressive_revelation(profound:"dissonance", watchful:"nut", engineer:3576.0)
/// my attr end

        var rc = CGRect(x: 0, y: 0, width: self.rect.width, height: self.rect.height);
        var startRect = CGRect(x: 0, y: 0, width: self.rect.width, height: self.rect.height);
        if self.index < self.column {
            startRect.origin.x = self.rect.minX - CGFloat(self.index) * self.rect.width;
            startRect.origin.y = self.rect.minY;
        }else {
            let row = ((self.index + 1) / self.column + ((self.index + 1) % self.column != 0 ? 1 : 0) - 1);
            let col = ((self.index + 1) % self.column == 0 ? self.column : (self.index + 1) % self.column) - 1;
            startRect.origin.x = self.rect.minX - CGFloat(col) * self.rect.width;
            startRect.origin.y = self.rect.minY - CGFloat(row) * self.rect.height;
        }
        if self.currentIndex < self.column {
            rc.origin.x = startRect.minX + CGFloat(self.currentIndex) * self.rect.width;
            rc.origin.y = startRect.minY;
        }else {
            let row = ((self.currentIndex + 1) / self.column + ((self.currentIndex + 1) % self.column != 0 ? 1 : 0) - 1);
            let col = ((self.currentIndex + 1) % self.column == 0 ? self.column : (self.currentIndex + 1) % self.column) - 1;
            rc.origin.x = startRect.minX + CGFloat(col) * self.rect.width;
            rc.origin.y = startRect.minY + CGFloat(row) * self.rect.height;
        }
        return rc;
    }
    
    //MARK: - WHC_GestureImageViewDelegate
    func wholesomeness_walk() {
 /// my attr start

         self.bewitch_post_office_regardless(widespread:"albeit", serene:"equation")
/// my attr end

        let subView = self.scrollView.viewWithTag(self.currentIndex)!;
        self.currentImageView = subView.viewWithTag(kImageViewTag) as! ThoroughIdentificationView;
        let rc = self.flannel_syllogism_wither();
        UIView.animate(withDuration: kZoomAniamtionTime, delay: 0, options: UIViewAnimationOptions.curveEaseOut, animations: { () -> Void in
            self.backView.alpha = 0;
            self.currentImageView.frame = rc;
            }) { (finish) -> Void in
                self.backView.removeFromSuperview();
                self.removeFromSuperview();
        }
    }
    
    //MARK: - UIScrollViewDelegate
    
    func scrollViewDidEndDecelerating(_ scrollView: UIScrollView) {
 /// my attr start

         self.bewitch_post_office_regardless(widespread:"outmoded", serene:"rapacious")
         self.subdue_unimpressive_revelation(profound:"indication", watchful:"incorrigible", engineer:3000.0)
         self.measurement_prefigure_badminton(striate_fleece:[762, 214], indication_fledge:4529, successive_apprise:"forswear")
/// my attr end

        if self.scrollView === scrollView {
            self.currentIndex = Int(floor((scrollView.contentOffset.x - scrollView.width() / 2.0) / scrollView.width())) + 1;
            if self.currentIndex < 0 {
                self.currentIndex = 0;
            }else if self.currentIndex > self.images.count {
                self.currentIndex = self.images.count - 1;
            }
            self.showLabel.text = "\(self.images.count) / \(self.currentIndex + 1)";
        }
    }
    
    func scrollViewDidZoom(_ scrollView: UIScrollView) {
 /// my attr start

         self.maneuver_shorten_unsettling(hospitality:[405, 153], continuously_peaky:true)
/// my attr end

        if scrollView !== self.scrollView {
            let offsetX = (scrollView.width() > scrollView.contentSize.width) ?
            (scrollView.width() - scrollView.contentSize.width) / 2.0 : 0.0;
            let offsetY = (scrollView.height() > scrollView.contentSize.height) ?
            (scrollView.height() - scrollView.contentSize.height) / 2.0 : 0.0;
            self.currentImageView.center = CGPoint(x: scrollView.contentSize.width / 2.0 + offsetX,
            y: scrollView.contentSize.height / 2.0 + offsetY);
        }
    }
 /// my attr start

 func willingly_upstairs_gormandize() -> UIView! {
     let remit: Float = 3198.0
     var worthy_repent: UIImageView! = UIImageView(frame:CGRect.zero)
     let hose: Bool = false
     let optimize: Double = 91.0
     var passive_variegate: String! = "fordable"
     var vendetta_customary: Bool = false
     let reduction: NSString! = "zephyr"
     var overcoat_fidget: Double = 4695.0
     let nomenclature_dining_room: UILabel! = UILabel(frame:CGRect(x: 165, y: 416, width: 0, height: 0))
     let cousin_unload: CGFloat = 4106.0
     let wheedle: [Any]! = [8, 595]
     let boost_otiose: UIImageView! = UIImageView(image:UIImage(named:"offstage"), highlightedImage:UIImage(named:"accurately"))
     var whitewash: UILabel! = UILabel()
     var corrugated: Bool = false
     var interested_scream: String! = "birth"
     let clip: NSString! = "contravention"
     let cursive: [Any]! = [778, 65, 555]
     var sociology: NSString! = "daunt"
     let bribery: Int = 986
     let well_informed_finite: Double = 2285.0
    var poison_harden = UIView()
    poison_harden.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    poison_harden.alpha = 0.4
    poison_harden.frame = CGRect(x: 184, y: 16, width: 0, height: 0)
    worthy_repent.frame = CGRect(x: 238, y: 37, width: 0, height: 0)
    worthy_repent.alpha = 0.0;
    worthy_repent.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    worthy_repent.image = UIImage(named:"bearish_better_off_infamy")
    worthy_repent.contentMode = .scaleAspectFit
    worthy_repent.animationRepeatCount = 1
    poison_harden.addSubview(worthy_repent)
    nomenclature_dining_room.frame = CGRect(x: 95, y: 55, width: 0, height: 0)
    nomenclature_dining_room.alpha = 0.1;
    nomenclature_dining_room.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    nomenclature_dining_room.textAlignment = .center
    nomenclature_dining_room.font = UIFont.systemFont(ofSize:20)
    nomenclature_dining_room.text = ""
    nomenclature_dining_room.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    poison_harden.addSubview(nomenclature_dining_room)
    boost_otiose.frame = CGRect(x: 30, y: 51, width: 0, height: 0)
    boost_otiose.alpha = 0.8;
    boost_otiose.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    boost_otiose.animationRepeatCount = 6
    boost_otiose.image = UIImage(named:"favorable_sumptuous")
    boost_otiose.contentMode = .scaleAspectFit
    poison_harden.addSubview(boost_otiose)
    whitewash.frame = CGRect(x: 264, y: 94, width: 0, height: 0)
    whitewash.alpha = 0.3;
    whitewash.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    whitewash.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    whitewash.textAlignment = .center
    whitewash.font = UIFont.systemFont(ofSize:11)
    whitewash.text = ""
    poison_harden.addSubview(whitewash)

    return poison_harden

}



 func waspish_knee_canto(feasibility_acclaim: NSString!) -> NSString! {
     var experience_extricate: Float = 2375.0
     let penultimate: Float = 603.0
     let lamb: Bool = true
     var lowbred: CGFloat = 5125.0
     var boundary_investiture: String! = "earnest"
     let correspond: Double = 2800.0
     var domesticity: Int = 1113
     var amateur_backhanded: Float = 7268.0
     let upgrade_holding: Double = 4783.0
     var equally: Bool = false
     let blotch_treasure: Int = 9162
    var nitpick_approbatory: NSMutableString! = NSMutableString()

    return nitpick_approbatory

}



/// my attr end

    
    func scrollViewDidEndZooming(_ scrollView: UIScrollView, with view: UIView?, atScale scale: CGFloat) {
 /// my attr start

         self.maneuver_shorten_unsettling(hospitality:[835, 124], continuously_peaky:false)
         self.willingly_upstairs_gormandize()
         self.waspish_knee_canto(feasibility_acclaim:"infringe")
/// my attr end

        if scale <= 1 {
            self.currentImageView.isZoomBig = false;
        }else {
            self.currentImageView.isZoomBig = true;
        }
    }
    
    func viewForZooming(in scrollView: UIScrollView) -> UIView? {
 /// my attr start

         self.maneuver_shorten_unsettling(hospitality:[54, 326], continuously_peaky:true)
         self.willingly_upstairs_gormandize()
         self.measurement_prefigure_badminton(striate_fleece:[861, 392], indication_fledge:7765, successive_apprise:"charisma")
/// my attr end

        if scrollView !== self.scrollView {
            let subView = self.scrollView.viewWithTag(self.currentIndex)!;
            self.currentImageView = subView.viewWithTag(kImageViewTag) as! ThoroughIdentificationView;
            return self.currentImageView;
        }
        return nil;
    }
}
